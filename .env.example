# SQLite for local/demo use — see README "Scaling to Postgres" to switch providers
DATABASE_URL="file:./dev.db"

# Optional — if unset, /api/insights serves the cached, human-validated
# insight set from data/fallback_insights.json instead of calling the API live
ANTHROPIC_API_KEY=
